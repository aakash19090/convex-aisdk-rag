## Steps to integrate AI Chatbox

- Create any API key from OpenAI or Gemini or any and add to Convex dashboard instance from `Settings > Environment Variables`.

- Create a server endpoint where you can request the Gemini API in `http.ts` file in convex folder
